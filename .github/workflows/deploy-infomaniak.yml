name: Deploy to Infomaniak

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: infomaniak-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npx oxlint
      - name: Build (production, root path)
        run: npm run build:prod
      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.INFOMANIAK_FTP_HOST }}
          username: ${{ secrets.INFOMANIAK_FTP_USERNAME }}
          password: ${{ secrets.INFOMANIAK_FTP_PASSWORD }}
          local-dir: dist/
          server-dir: /
          # Wipes the remote root and re-uploads dist/ in full on every
          # deploy — the site is small (a few MB) so the extra transfer
          # time is negligible, and it removes any doubt about stale
          # hashed assets surviving a broken sync-state file under the
          # 10 MB Infomaniak hosting quota.
          dangerous-clean-slate: true
